// Type: number aceita: int, float, hex, binary

let valor1: number = 20.100; // aceita valores diretos (não só o type number)

// Typescript já faz o ajuste da quantidade de casas decimais necessárias na exibição sem código extra (incrível)

let valor2: number;

valor2 = 10;

console.log(valor1 + valor2);